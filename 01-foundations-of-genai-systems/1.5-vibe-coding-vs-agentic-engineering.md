# Vibe Coding vs. Agentic Engineering

*The difference is not whether AI writes code—it is how intent becomes verified software*

_Estimated reading time: 6 minutes._

AI-assisted implementation exists on a spectrum. At one end, a developer describes a desired result, accepts generated code quickly, and checks it by interacting with the product. At the other, AI works inside specifications, tests, security boundaries, and review gates. Both modes can be useful. The mistake is treating prototype-level verification as production evidence.

## Two Modes of AI-Assisted Development

  ### Vibe Coding

- Optimize for speed of exploration
- Describe intent informally
- Accept broad generated changes
- Debug through conversational trial and error
- Verify mainly through manual inspection
- Appropriate for disposable prototypes and low-risk experiments

  ### Agentic Engineering

- Translate intent into explicit requirements and constraints
- Limit the environment and tools available to the agent
- Use deterministic tests for code behavior
- Use evals for probabilistic or model-mediated behavior
- Require review and deployment gates
- Appropriate when the result must be maintained, audited, or trusted

These are not identities or opposing ideologies. A team may vibe-code a throwaway prototype to learn what users want, then convert the validated idea into an agentic engineering process.

## Verification Is the Boundary

The core difference is not prompt sophistication. It is whether success is measured through repeatable evidence.

| Artifact | How to verify it |
|---|---|
| Deterministic business logic | Unit, integration, and property-based tests |
| Generated structured output | Schema validation and contract tests |
| RAG answer | Retrieval tests, groundedness checks, citation validation, human review |
| Agent behavior | Task success, tool arguments, trajectory checks, stop-condition tests, cost limits |
| Security controls | Permission tests, adversarial cases, sandbox and policy validation |

> **Litmus test:** If the only evidence is "I clicked around and it looked right," the work is still at prototype verification—even if the prompt and model are sophisticated.

## Specifications Become More Important

When implementation is cheap, ambiguity becomes expensive. An agent can generate thousands of lines in the wrong direction faster than a human can review them. A useful specification therefore describes:

- The user problem and intended outcome
- Functional and non-functional requirements
- Allowed dependencies and architectural boundaries
- Acceptance criteria and eval cases
- Security, privacy, and operational constraints
- What the agent may change without approval

Chapter 5 will turn this into a complete spec-driven production workflow. Here, the foundational point is simple: AI increases implementation speed, so clear intent and verification must scale with it.

## Review the Trajectory, Not Only the Final Diff

A final result can appear correct while the process was unsafe. An agent might disable a test, bypass an authorization check, or use an unapproved dependency to make the visible output pass. Reviewing only the final screen misses these failures.

Trajectory evidence can include:

- Files read and modified
- Commands and tools executed
- Tests run and tests skipped
- Permission decisions
- Intermediate plans and revisions
- Token, time, and cost consumed

Trajectory review does not mean reading every hidden reasoning token. It means observing the externally meaningful actions and state transitions the system performed.

## Prototype Debt

Vibe coding has low startup cost, but repeated trial and error can create prompt patches, duplicated logic, unclear ownership, untested generated code, and fragile dependencies. This is **prototype debt**: decisions optimized for discovery become liabilities when the prototype quietly turns into production.

> **Analogy:** A prototype is a sketch used to decide what building to construct. Agentic engineering is the structural plan, inspection process, and controlled construction site required to let people safely live inside it.

## Key Takeaways

- Vibe coding optimizes exploration; agentic engineering optimizes repeatability and trust.
- The boundary is systematic verification, not whether AI generated the code.
- Fast generation increases the value of specifications and acceptance criteria.
- Review observable trajectories and side effects, not only the final output.
- Prototype practices should be deliberately replaced before production.

<details>
<summary><strong>Check your understanding:</strong> A team uses an AI coding agent, requires a written spec, runs unit and integration tests, checks the agent's file changes, and blocks deployment when eval thresholds fall. Which mode best describes the process?</summary>

- **A.** Vibe coding, because AI generated code
- **B.** Manual development, because tests are present
- **C.** Agentic engineering
- **D.** Multi-agent architecture

**Answer: C.** The correct answer is C. The process turns intent into constrained execution and repeatable evidence rather than relying on an eyeball test.

</details>
